# 260909 작업일지

## 브랜치
`feature/residue-detection`

---

## 작업 내역

### 1. 잔류물 감지 모듈 구현 (`1c7eb73`)
- `include/residue.h`, `src/residue.c` 신규 작성
- 배경 차분 기반 후보 블록 산출 → 4-이웃 연결 요소 → 영역화
- 사람 bbox 배제, 조명 변화 가드 (global_change_ratio), 지속 시간 확정, 위치 기반 분류(trash/spill)
- 안전 시점 자동 기준 갱신 (사람 없음 + 후보 없음 + 주기 경과)

### 2. 단위 테스트 수정 (`9478241`)
- `global_change_ratio=1.1f`로 변경 — 1×1 블록 테스트에서 조명 가드 오발화 방지
- `test_residue_confirms_after_hold`, `test_residue_clears_after_absence`: 초기 `residue_evaluate` 호출로 영역 먼저 등록

### 3. 단일 인스턴스 잠금 (`8c34312`)
- `include/platform.h` / `src/platform.c`: `platform_single_instance_try_lock()` / `platform_single_instance_unlock()` 추가
- Windows: 세션 뮤텍스 `"hunik-detector-lock"` (Global\ 미사용 — 관리자 권한 불필요)
- Linux: `flock(LOCK_EX | LOCK_NB)` on `/tmp/hunik-detector.lock`
- 세 번째 프로세스 실행 시 즉시 오류 종료

### 4. run-all 카메라 선택 메뉴 (`aa99070`)
- `run-all.bat` (4줄) + `run-all.ps1` (로직 전체) 분리 — CMD 따옴표 중첩 오류 우회
- ffmpeg dshow로 카메라 목록 자동 열거
- 1대 → 자동 선택, 2대 이상 → 번호 메뉴로 선택

### 5. run-all 버그 수정 3건 (`603d74f`, `eb18f5d`, `88a03ad`)
- `$ErrorActionPreference = 'Continue'` + `.ToString()` — ffmpeg stderr가 ErrorRecord로 중단되던 문제 수정
- `[Console]::OutputEncoding = UTF8` — 한국어 출력 깨짐 수정
- `--camera-size 1280x720 --camera-fps 15` 추가 — QHD 웹캠 dshow 30MB 버퍼 넘침 수정

### 6. SQLite 이벤트 로그 + CPU 주기 로깅 (`d9b306c`)
- `include/log.h`, `src/log.c`: `FILE*` 텍스트 로그 → SQLite WAL 모드 DB로 완전 교체
  - API: `event_log_open()`, `event_log_close()`, `event_log_write()`, `event_log_count()`
  - `:memory:` 지원 (테스트용 파일 없이 동작)
- `third_party/sqlite/sqlite3.c`, `sqlite3.h`: SQLite 3.49.1 amalgamation 추가
- `src/main.c`: 30초마다 `cpu_usage=X% wall=Y.Ys cpu=Z.Zs` 기록 (`perf` 모듈)
- `run-all.ps1`: 로그 확장자 `.log` → `.db` 변경
- `tests/test_core.c`: `event_log_init` + `FILE*` + `ftell` → `event_log_open(":memory:")` + `event_log_count()` 전면 교체
- `src/residue.c`: `residue_cleared` 이벤트 `LOG_INFO` → `LOG_WARN` 수정
- **최종 테스트: 60/60 통과**

---

## 기술 결정 사항

| 항목 | 결정 | 이유 |
|---|---|---|
| 이벤트 로그 형식 | SQLite WAL | 대시보드가 로그 읽는 동안 감지가 차단되지 않아야 함 |
| 로그 확장자 | `.db` | SQLite 파일임을 명시 |
| 세션 뮤텍스 | `Global\` 미사용 | 관리자 권한 없이도 동작해야 함 |
| 카메라 해상도 상한 | 1280×720@15fps | 모델 입력 416×224 수준, QHD는 추론에 기여 없고 버퍼만 넘침 |
| CPU 로깅 주기 | 30초 | 너무 잦으면 로그 노이즈, 너무 길면 스로틀링 파악 늦음 |

---

## 테스트 결과

```
--- 60 passed, 0 failed ---
```

빌드 대상: `yolo11-person.exe`, `hunik-dashboard.exe`, `test_core.exe` 모두 정상

---

## 대시보드 수정 (`9a2e21e`)

### server.c
- 로그 파일 탐색 `*.log` → `*.db`
- 텍스트 파서(`parse_log_line`, `line_to_json`) 제거
- `serve_history`: SQLite `SELECT ts_iso, level, module, message FROM events ORDER BY id`
- `serve_stream`: 400ms 간격 폴링, 매 주기마다 DB 재열기 (WAL 최신 행 즉시 반영)
- `is_db_file()` 검증 추가

### CMakeLists.txt
- `hunik-dashboard` 타겟에 `third_party/sqlite/sqlite3.c` 소스 추가

### index.html
- 사이드바 **잔류물 감지** 카운터 (trash·spill) 추가
- `residue_trash` / `residue_unknown` → 주황 강조선
- `residue_spill` → 빨강 강조선
- `residue_cleared` → 초록 강조선
- `countEvent()` / `rowClass()` 잔류물 분기 추가

---

## 미결 사항

- [ ] 의자 위 잔류물 오탐 — 의자 bbox 안 잔류물도 `RESIDUE_TRASH`로 분류됨. 손님이 가방을 1분 넘게 의자 위에 두면 오탐 발생. 차후 해결 예정 (의자 제외 또는 별도 확정 시간 적용)
